<UserControl x:Class="DemoDesktopApp.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DemoDesktopApp.Views"
             xmlns:viewmodels="clr-namespace:DemoDesktopApp.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:SettingsViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel HorizontalAlignment="Left">
            <TextBlock Text="Settings" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBlock Text="Tally Base URL:"/>
            <TextBox Text="{Binding TallyBaseUrl}" Width="200" HorizontalAlignment="Left" Margin="0,0,0,10"/>

            <TextBlock Text="Tally Port:"/>
            <TextBox Text="{Binding TallyPort}" Width="200" HorizontalAlignment="Left" Margin="0,0,0,10"/>

            <Button Content="Save" Command="{Binding SaveCommand}" Width="100" HorizontalAlignment="Left" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
